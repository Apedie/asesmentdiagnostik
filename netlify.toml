[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — semua route diarahkan ke index.html agar React Router bekerja
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
